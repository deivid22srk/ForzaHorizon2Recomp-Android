# recompiled_functions.toml — XenonAnalyse jump-table output for FH2.
#
# REAL RESULT (2026-09-15, retail default.xex, XenonAnalyse built from
# hedge-dev/XenonRecomp): ZERO switch tables matched. Exit 0, output contains
# only the section headers. FH2's compiler (newer Xbox XDK, LTCG) emits
# switch/jump sequences that match none of XenonAnalyse's 4 hardcoded
# patterns (absolute/computed/byte-offset/word-offset).
#
# Consequence: the recompiler ran WITHOUT switch tables and still completed
# 100% (jump tables decoded as data-in-code instead — see the 7 "Unable to
# decode" at 0x832F1A78.. in docs/recomp.md). Code correctness for
# switch-heavy code (UI menus, state machines) needs MANUAL switch analysis:
# disassemble computed jumps (mtctr/bctr clusters) and add entries here in
# the tool's schema:
#   [[switch]]
#   base = 0x...
#   r = ...
#   default = 0x...
#   labels = [ 0x..., ... ]

[switch_tables]
# populated by XenonAnalyse (empty for this binary — see note above)

[functions]
# Manual stubs (analyser misses), one line per stub with reason + date:
# 0xADDR = { name = "sub_ADDR" }  # reason
